apiVersion: v2
name       : countour-ingress-deployment
description: Configures ingress controller
type       : application
version    : 0.1.0   # This is the Helm Chart version
appVersion : "1.0.0" # This is the version of the application being deployed

keywords:
  - marketplace-boilerplate
  - http
  - https