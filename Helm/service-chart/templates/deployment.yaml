apiVersion : apps/v1
kind: Deployment
metadata:
  name: {{ .Values.serviceName }}-{{ .Values.Env }}-deployment
  labels:
    app : {{ .Values.serviceName }}-{{ .Values.Env }}-deployment
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      project: {{ .Values.serviceName }}-{{ .Values.Env }}
  template:
    metadata:
      labels:
        project: {{ .Values.serviceName }}-{{ .Values.Env }}   # Service will look for those PODS Labels!!!
    spec:
      containers:
        - name : {{ .Values.serviceName }}-{{ .Values.Env }}
          image: {{ .Values.Image }}
          imagePullPolicy: Always
          ports:
            - containerPort: 80